---
number: 1
title: Finish fix-loop token-efficiency: runtime measurements + corpus + spec follow-ups
labels: [enhancement, P2]
status: closed
---
Spec: docs/specs/2026-05-21-fix-loop-token-efficiency-design.md

## Shipped on this branch (feat/fix-loop-token-efficiency)

- ✅ Phase 1: --if-unset atomic claim (4 tests pass, incl. 10-process race)
- ✅ Phase 2.0 deterministic subset: gate-log.py, analyze-gate-log.py, golden fixtures, schema-check
- ✅ Phase 0 default: cadence 15m→4m, 1h-TTL probe runbook
- ✅ Queue-state fixtures (empty/p2-bug/triage/active)
- ✅ §7.1.1: Task model= override verified (A2 confirmed)
- ✅ Phase 2: /autocoder:gate slim command (1973 B, ~38× smaller than fix.md)
- ✅ Phase 3: /autocoder:dispatch Haiku-pinned variant, opt-in via LOOP_MODEL_SPLIT=1

## Remaining work

### Autonomously implementable (the autocoder loop can do these)

1. **Triage test corpus.** Author 10–20 issue bodies under tests/triage_corpus/ with `expected.csv` mapping each to its correct P0–P3 label. Used by §13.4 Phase 3 acceptance (Haiku-vs-Sonnet ≥80% agreement).
2. **Phase 2 tests.** /autocoder:gate currently has no tests. Add a unit test that exercises the bash-block of gate.md against a temp work.json with each phase value and asserts the right branch is taken (mock the Skill/Task invocations).
3. **Phase 1 unit-test markers.** Mark tests/test_issues_file_if_unset.py with pytestmark=unit so it runs in the fast suite per repo convention (see other test files for the pattern).
4. **--inject-at protocol.** scripts/run-phase-experiment.sh advertises --inject-at but the handler isn't wired. Implement: fork a background sleeper that copies inject-fixture contents into .issues/ at the specified offset.
5. **Cumulative spec roll-up template.** Create docs/reports/TEMPLATE-cumulative.md per §13.5 so future engineers running cumulative reports have a starting point.
6. **gate.md / dispatch.md model-frontmatter convention check.** Other autocoder commands don't use frontmatter model: declarations; verify this is the right pattern for Claude Code by reading the marketplace docs or experimentation.

### Requires runtime experiment (NOT implementable in /fix-loop)

These need actual /loop sessions, Claude Code telemetry, or 48-hour wall-clock. Skip these in autocoder runs:

- **Phase 2.0 PostSessionEnd hook probe.** Determine whether Mode 1 (/loop / CronCreate) fires session-end hooks; if not, gate-log.py needs the next-tick-gate-entry fallback per §13.1.
- **Phase 2.0 TTL calibration.** Run §13.1 #3 TTL experiment three times across off-peak/peak/weekend over ≤48 hours; publish range.
- **Phase 0 measurement.** Use run-phase-experiment.sh against the empty fixture to compare 4m/5m-TTL vs 55m/1h-TTL; commit the cheaper as default.
- **Phase 2 acceptance measurement.** Per §13.4: per-tick input tokens ≤15% of pre-Phase-2 baseline on a cache-warm fixture.
- **Phase 3 acceptance measurement.** Triage correctness ≥80% of Sonnet baseline on the corpus from item 1; per-routing-tick input cost ≤30% of Phase 2 baseline.
- **Phase 4 slim fix.md.** Gated on Phase 2 acceptance measurement landing first. Per §13.4 Phase 4: per-active-tick total cost ≤50% of pre-Phase-4 baseline + behavioral oracle + diff-scope check.

## Done criteria for THIS issue

- Items 1–6 implemented and committed
- Issue notes which runtime experiments are still outstanding (handed off to a human or future runtime session)
> **2026-05-21 23:20** 🤖 **Automated Fix Started**

Decomposing this issue into the 6 autonomously-implementable sub-items. Starting with item 1 (triage corpus) since it's the cleanest standalone chunk.

Branch: `feat/fix-loop-token-efficiency` (continuing on existing branch — all sub-items are linked work, no point in branching per item).
Started: Thu May 21 19:20:32 EDT 2026

Plan:
1. Subagent: author triage corpus (10 issue bodies + expected.csv)
2. Subagent: add pytestmark=unit to Phase 1 tests
3. Subagent: wire --inject-at handler in run-phase-experiment.sh
4. Subagent: gate.md unit test
5. Subagent: cumulative report template
6. Subagent: model-frontmatter convention check

Runtime experiments (PostSessionEnd probe, TTL calibration, Phase 2/3/4 acceptance measurements) stay unhandled — they need a live Claude Code loop session, not autocoder.
> **2026-05-21 23:30** ✅ **Issue Resolved (autonomous items)**

All 6 autonomously-implementable sub-items shipped on \`feat/fix-loop-token-efficiency\`:

- ✅ Item 1: triage corpus (10 issues + expected.csv) — commit 29e448d
- ⏭ Item 2: pytestmark=unit — SKIPPED. Agents repo doesn't register a 'unit' marker; adding one would just emit PytestUnknownMarkWarning. Documented in commit d3b4b77.
- ✅ Item 3: --inject-at handler wired in run-phase-experiment.sh — commit d3b4b77
- ✅ Item 4: gate.md bash-skeleton tests + structural guard (14 tests, all pass) — commit 134c73b. Honest scope: gate.md's routing is LLM-prose, so per-phase functional assertions need an integration test.
- ✅ Item 5: docs/reports/TEMPLATE-cumulative.md — commit d3b4b77
- ✅ Item 6: dispatch.md model frontmatter fixed (claude-haiku-4-5 → haiku) per Claude Code docs + bundled-plugin convention — commit 134c73b's successor

**Runtime experiments still outstanding** (cannot be done in autocoder loop; require live Claude Code session + wall clock):
- Phase 2.0 PostSessionEnd hook probe
- Phase 2.0 TTL calibration (48-hour, three times-of-day)
- Phase 0 4m/5min-TTL vs 55m/1h-TTL measurement
- Phase 2 / 3 / 4 acceptance measurements
- §7 A1 (Skill inheritance) re-test if Phase 3 design changes
- Phase 4 slim worker (gated on Phase 2 acceptance)

These are tracked here but not implementable autonomously. Handing off to a runtime session.

Branch: \`feat/fix-loop-token-efficiency\` (12 commits ready for review/push)
