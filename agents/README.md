# OpenCode Agents

This directory contains agent definitions and prompts for the **OpenCode** platform.

## Structure

- **`autocoder/`**: Configuration for the Autocoder agent (bug resolution).
- **`modernize/`**: Configuration for the Modernize agent (legacy migration).
