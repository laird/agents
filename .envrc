export CLAUDE_CODE_TASK_LIST_ID="agents"

# Ship branch (issue #35 ship gate, #37).
# The swarm's shipping line is the integration branch, NOT master: nothing this
# swarm produces reaches master until the /dev line merges (#30). Measuring
# "shipped" against master would leave every fixed issue open behind that one
# merge. Change this when the /dev line lands and master becomes the ship line.
export CLAUDE_CODE_SHIP_BRANCH="feat/autocoder-planning-pipeline"
